# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

GET		/uri						controllers.URIController.byUriGET
GET		/structure					controllers.FileUploadController.byStructureFileUploadGET
POST	/structure					controllers.FileUploadController.byStructureFileUploadPOST
POST	/observations				controllers.FileUploadController.byObservationFileUploadPOST